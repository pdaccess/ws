services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: pda
      POSTGRES_PASSWORD: pda
      POSTGRES_DB: pda
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: